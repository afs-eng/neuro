services:
  - type: web
    name: neuroavalia-backend
    env: python
    plan: free
    rootDir: .
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    preDeployCommand: python manage.py migrate
    startCommand: gunicorn config.wsgi:application --log-file -
    healthCheckPath: /healthz/
    envVars:
      - key: DJANGO_ENV
        value: production
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        fromDatabase:
          name: neuroavalia-db
          property: connectionString
      - key: DATABASE_SSL_REQUIRE
        value: "True"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: neuroavalia-backend.onrender.com
      - key: CSRF_TRUSTED_ORIGINS
        value: https://neuroavalia-backend.onrender.com,https://your-vercel-app.vercel.app
      - key: CORS_ALLOWED_ORIGINS
        value: https://your-vercel-app.vercel.app
      - key: FRONTEND_BASE_URL
        value: https://your-vercel-app.vercel.app
      - key: BACKEND_PUBLIC_URL
        value: https://neuroavalia-backend.onrender.com
      - key: EMAIL_BACKEND
        value: django.core.mail.backends.smtp.EmailBackend
      - key: DEFAULT_FROM_EMAIL
        value: no-reply@neuroavalia.test
databases:
  - name: neuroavalia-db
    plan: free
